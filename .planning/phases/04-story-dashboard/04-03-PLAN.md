---
phase: 04-story-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/src/components/FeedbackForm.tsx
  - frontend/src/components/FeedbackButton.tsx
  - frontend/src/components/LoadingSkeleton.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/incidents/[id]/page.tsx
  - frontend/src/components/StoryCard.tsx
  - frontend/src/components/IncidentCard.tsx
autonomous: false
requirements: [DASH-04, DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "Analyst can submit thumbs up or thumbs down on any narrative"
    - "Analyst can add optional text notes with feedback"
    - "Feedback is persisted to database and visible on reload"
    - "Toast notification confirms successful feedback submission"
    - "Already-submitted feedback shows current state (up/down selected)"
    - "Loading skeletons display during all data fetches"
    - "Error states shown when API calls fail"
    - "All 4 DASH requirements verified end-to-end"
  artifacts:
    - path: "frontend/src/components/FeedbackForm.tsx"
      provides: "Thumbs up/down + notes feedback form"
      contains: "FeedbackForm"
    - path: "frontend/src/components/FeedbackButton.tsx"
      provides: "Toggle button to show/hide feedback form"
      contains: "FeedbackButton"
    - path: "frontend/src/components/LoadingSkeleton.tsx"
      provides: "Skeleton loading states for all major components"
      contains: "LoadingSkeleton"
  key_links:
    - from: "frontend/src/components/FeedbackForm.tsx"
      to: "frontend/src/hooks/useFeedback.ts"
      via: "uses useFeedback hook for submission"
      pattern: "useFeedback"
    - from: "frontend/src/components/StoryCard.tsx"
      to: "frontend/src/components/FeedbackForm.tsx"
      via: "renders feedback form at bottom"
      pattern: "FeedbackForm"
    - from: "frontend/src/components/FeedbackForm.tsx"
      to: "http://localhost:8080/api/feedback"
      via: "POST request via useFeedback"
      pattern: "submitFeedback"
---

<objective>
Complete the feedback system, add loading/error states, and verify all Phase 4 requirements end-to-end.

Purpose: The feedback loop is the final user interaction — analysts rate narrative quality to improve the AI. Loading states ensure the UI feels polished. End-to-end verification confirms all requirements are met.
Output: Working feedback form, polished loading/error states, verified dashboard.
</objective>

<execution_context>
@/home/piyuxhh/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/piyuxhh/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-story-dashboard/04-01-SUMMARY.md
@.planning/phases/04-story-dashboard/04-02-SUMMARY.md
@.planning/phases/04-story-dashboard/04-RESEARCH.md
@frontend/src/lib/types.ts
@frontend/src/lib/api.ts
@frontend/src/lib/utils.ts
@frontend/src/components/StoryCard.tsx
@frontend/src/hooks/useFeedback.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feedback Form + Button + Loading Skeletons</name>
  <files>
    frontend/src/components/FeedbackForm.tsx,
    frontend/src/components/FeedbackButton.tsx,
    frontend/src/components/LoadingSkeleton.tsx
  </files>
  <action>
1. Create `frontend/src/components/FeedbackForm.tsx` as a Client Component ('use client'):
   - Props: `narrativeId: number`, `incidentId: number`, `existingFeedback: Feedback | null`
   - Use `useFeedback` hook from @/hooks/useFeedback
   - If existingFeedback is not null: show read-only state with current rating highlighted and notes displayed
   - If no existingFeedback: show interactive form:
     - Question: "Was this narrative helpful?" in text-sm text-zinc-400
     - Rating buttons: ThumbsUp and ThumbsDown icons from lucide-react
       - Unselected: bg-zinc-800 text-zinc-400 hover:bg-zinc-700
       - Selected up: bg-green-900/30 text-green-400 border border-green-700
       - Selected down: bg-red-900/30 text-red-400 border border-red-700
     - Notes textarea: placeholder "Optional notes about this narrative...", bg-zinc-800 border border-zinc-600 rounded p-2 text-sm, 3 rows, resize-none
     - Submit button: "Submit Feedback", bg-blue-600 hover:bg-blue-500, disabled when no rating selected or submitting
   - On submit: call submitFeedback from useFeedback hook
   - On success: show toast "Feedback submitted" via react-hot-toast (import toast from 'react-hot-toast')
   - On error: show toast "Failed to submit feedback" in error variant
   - Layout: bg-zinc-900 border border-zinc-800 rounded-xl p-6 mt-6

2. Create `frontend/src/components/FeedbackButton.tsx` as a Client Component:
   - Props: `narrativeId: number`, `incidentId: number`, `existingFeedback: Feedback | null`
   - State: `isExpanded: boolean` (default false)
   - Render: button that toggles feedback form visibility
     - Collapsed: "Rate this narrative" with MessageSquare icon from lucide-react, bg-zinc-800 hover:bg-zinc-700
     - If existingFeedback: show "Feedback submitted" with CheckCircle icon in text-green-400 instead
   - When expanded: render FeedbackForm component below the button
   - Smooth expand/collapse with max-height transition or conditional render

3. Create `frontend/src/components/LoadingSkeleton.tsx` with exported skeleton components:
   - `IncidentCardSkeleton()` — pulse rectangle matching IncidentCard dimensions
   - `StoryCardSkeleton()` — pulse rectangle with 3 sentence-like bars
   - `RawEventViewerSkeleton()` — 3 small pulse bars matching event card layout
   - `PageSkeleton()` — full page skeleton with header + content area
   - All skeletons: bg-zinc-800 animate-pulse rounded, appropriate sizing
  </action>
  <verify>
    <automated>cd /home/piyuxhh/hackathon/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - FeedbackForm renders interactive form for new feedback
    - FeedbackForm shows read-only state for existing feedback
    - FeedbackButton toggles form visibility
    - Toast notification shown on successful submission
    - Toast error shown on failed submission
    - LoadingSkeleton components render for all major UI areas
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-End Integration Verification</name>
  <what-built>
Complete NarratorAI Story Dashboard with:
- Incident list page showing all incidents with severity badges and technique labels
- Incident detail page with story card, narrative sentences, confidence scores
- Hover-to-reveal source tracing (hover sentence → see raw events)
- Feedback system (thumbs up/down + notes with toast notifications)
- Dark theme SOC analyst dashboard
  </what-built>
  <how-to-verify>
**Prerequisites:**
1. Backend running: `cd backend && go run main.go` (port 8080)
2. Frontend running: `cd frontend && npm run dev` (port 3000)
3. Data imported: `curl -X POST http://localhost:8080/api/import`
4. Incidents grouped: `curl -X POST http://localhost:8080/api/incidents/group`
5. At least one narrative generated: `curl -X POST http://localhost:8080/api/incidents/1/narrative`

**Verification Steps:**
1. Open http://localhost:3000 — Incident list loads with cards showing severity badges (DASH-01 ✓)
2. Each card shows: severity indicator, title, description, technique labels, event count (DASH-01 ✓)
3. Click any incident card — navigates to /incidents/[id] detail page
4. Detail page shows: title, metadata (source IP, time range, event count), MITRE techniques
5. Story card displays narrative sentences with confidence scores and technique labels (DASH-02 ✓)
6. Hover over any narrative sentence — sentence highlights with blue left border
7. Right panel shows source events for the hovered sentence (DASH-03 ✓)
8. Source events show: timestamp, hostname, source IP, event type, process info
9. Move hover to different sentence — panel updates to show different source events
10. Click "Rate this narrative" button — feedback form appears
11. Select thumbs up or thumbs down — button highlights in green/red
12. Type optional notes in textarea
13. Click "Submit Feedback" — toast notification confirms submission (DASH-04 ✓)
14. Reload page — feedback persists and shows as read-only
15. Navigate back to home — back button works

**Expected Results:**
- All 4 success criteria from ROADMAP.md are met
- Dark theme applied consistently across all pages
- No console errors in browser developer tools
- Responsive layout works on desktop viewport
  </how-to-verify>
  <resume-signal>Type "approved" if all 15 verification steps pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Go API | Feedback submission sends user notes |
| Feedback notes → DOM | Display of analyst notes in read-only form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | feedback.notes (client) | mitigate | Max 1000 chars enforced client + server, React auto-escapes on render |
| T-04-07 | Repudiation | Feedback submission | accept | No auth in demo mode, feedback is non-critical data |
| T-04-08 | Denial of Service | Toast stack | mitigate | react-hot-toast limits visible toasts, auto-dismiss after 3s |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — TypeScript compiles
- `cd frontend && npm run build` — Next.js build succeeds
- Human verification passes all 15 steps
</verification>

<success_criteria>
- Feedback form accepts thumbs up/down and optional notes
- Feedback persists to database and displays on reload
- Toast notifications for success and error states
- Loading skeletons display during data fetches
- All 4 DASH requirements verified end-to-end by human
- Dashboard is visually polished with consistent dark theme
</success_criteria>

<output>
Create `.planning/phases/04-story-dashboard/04-03-SUMMARY.md` when done
</output>
